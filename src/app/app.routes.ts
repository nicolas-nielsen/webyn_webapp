import { Routes } from '@angular/router';
import { TrafficComponent } from 'feature/page/traffic/traffic.component';

export const routes: Routes = [
  { path: 'traffic', component: TrafficComponent },
];
